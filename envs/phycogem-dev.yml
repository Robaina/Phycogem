name: phycogem-dev
channels:
  - defaults
  - bioconda
  - conda-forge
dependencies:
  - python >= 3.8
  - pip
  - numpy
  - pandas
  - nextflow
  - pip:
    - poetry
    - cobra
    - ipykernel
    - ruff
    - black
    - coverage
