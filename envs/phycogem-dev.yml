name: phycogem-dev
channels:
  - defaults
  - bioconda
  - conda-forge
dependencies:
  - python >= 3.8
  - pip
  - numpy
  - pandas
  - cobra
  - nextflow
  - pip:
    - poetry
    - ipykernel
    - ruff
    - black
    - coverage
